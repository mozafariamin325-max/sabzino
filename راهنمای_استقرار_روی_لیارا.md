# راهنمای استقرار سبزینو روی Liara (به‌جای PythonAnywhere + Vercel)

این راهنما نسخهٔ «واقعی» سبزینو (فاز ۸ — درخواست واقعی، پذیرش/رد واقعی راننده، داشبورد مدیریت کامل) را از صفر روی **Liara** مستقر می‌کند. لیارا یک ابر ایرانی است؛ یعنی سرعت و پایداری آن از داخل ایران بسیار بهتر از PythonAnywhere/Vercel (که هر دو خارج از ایران هستند) خواهد بود.

> هر جا عبارت `شما` آمده یعنی مقداری است که خودتان باید جایگزین کنید (مثل رمز عبور یا دامنه).

---

## ۰. پیش‌نیازها

1. یک حساب کاربری در [liara.ir](https://liara.ir) بسازید و اعتبار اولیه (شارژ) تهیه کنید.
2. Node.js را روی سیستم خودتان نصب داشته باشید (برای نصب CLI لیارا).
3. نصب CLI لیارا:
   ```bash
   npm install -g @liara/cli
   liara login
   ```

---

## ۱. ساخت پایگاه‌دادهٔ Postgres

1. در پنل لیارا → **Postgres** → **ایجاد دیتابیس جدید** (مثلاً نام `sabzino-db`).
2. بعد از ساخته‌شدن، وارد صفحهٔ همان دیتابیس شوید و از بخش «اطلاعات اتصال» این ۵ مقدار را یادداشت کنید:
   - `Host` (میزبان)
   - `Port` (پورت)
   - `Database Name` (نام دیتابیس)
   - `Username` (نام کاربری)
   - `Password` (رمز عبور)
3. با این ۵ مقدار، رشتهٔ اتصال زیر را خودتان بسازید (این فرمت استاندارد Postgres است و بک‌اند سبزینو مستقیماً همین را می‌خواند):
   ```
   postgres://USERNAME:PASSWORD@HOST:PORT/DATABASE_NAME
   ```

---

## ۲. استقرار بک‌اند (Django)

پوشهٔ `backend/` همین حالا برای لیارا آماده است (فایل `backend/liara.json` از قبل ساخته شده و `requirements.txt` شامل `gunicorn` و `psycopg2-binary` است).

1. در پنل لیارا یک اپ جدید با پلتفرم **Django** بسازید (مثلاً نام `sabzino-backend`) — یا مستقیم از CLI در مرحلهٔ دیپلوی ساخته می‌شود.
2. متغیرهای محیطی زیر را از بخش **Environment Variables** همان اپ تنظیم کنید:

   | نام متغیر | مقدار |
   |---|---|
   | `SECRET_KEY` | یک رشتهٔ تصادفی طولانی و امن (مثلاً با `python -c "import secrets; print(secrets.token_urlsafe(50))"` بسازید) |
   | `DEBUG` | `False` |
   | `ALLOWED_HOSTS` | `sabzino-backend.liara.run` (دامنهٔ پیش‌فرض لیارا؛ بعداً اگر دامنهٔ اختصاصی گرفتید همان را هم اضافه کنید) |
   | `DATABASE_URL` | همان رشتهٔ اتصالی که در مرحلهٔ ۱ ساختید |
   | `CORS_ALLOW_ALL` | `False` |
   | `CORS_ALLOWED_ORIGINS` | آدرس فرانت‌اند شما، مثلاً `https://sabzino-frontend.liara.run` |
   | `CSRF_TRUSTED_ORIGINS` | همان آدرس فرانت‌اند، مثلاً `https://sabzino-frontend.liara.run` |
   | `MEDIA_ROOT` | `/media` (به‌شرط ساخت دیسک در مرحلهٔ بعد) |

3. **ساخت دیسک برای فایل‌های آپلودی (عکس درخواست‌ها، مدارک راننده‌ها):**
   فضای فایل هر اپ در لیارا با هر بار دیپلوی جدید پاک می‌شود؛ برای اینکه عکس‌ها و مدارک آپلودی گم نشوند باید یک **Disk** دائمی بسازید و آن را روی مسیر `/media` مانت (متصل) کنید. این کار از پنل لیارا (بخش دیسک‌های همان اپ) یا با فلگ `--disks` در دستور `liara deploy` انجام می‌شود؛ چون این بخش در مستندات رسمی لیارا به‌طور کامل مثال نداشت، پیشنهاد می‌شود دقیقاً طبق راهنمای آنی بخش «Disk» در پنل لیارا برای اپ Django عمل کنید — نام دیسک را مثلاً `sabzino-media` و مسیر مانت را `/media` بگذارید.

4. دیپلوی بک‌اند از داخل پوشهٔ `backend/`:
   ```bash
   cd backend
   liara deploy --app sabzino-backend --platform django --port 8000
   ```
   لیارا به‌طور خودکار `requirements.txt` را نصب، `collectstatic` را اجرا و برنامه را با gunicorn بالا می‌آورد.

5. **اجرای migrate و ساخت دادهٔ دمو** — از پنل لیارا وارد بخش **Shell** همان اپ بک‌اند شوید (یا با CLI: `liara shell --app sabzino-backend`) و دستورهای زیر را بزنید:
   ```bash
   python manage.py migrate
   python manage.py createsuperuser
   python manage.py seed_demo   # فقط اگر می‌خواهید داده‌های نمونه/آزمایشی هم داشته باشید
   ```

---

## ۳. استقرار فرانت‌اند (React)

پوشهٔ `frontend/` نیز آمادهٔ لیارا است (فایل `frontend/liara.json` با `platform: "react"` ساخته شده و خروجی ساخت (`build`) هم در `vite.config.ts` روی `build/` تنظیم شده، چون پلتفرم React لیارا این نام پوشه را انتظار دارد).

1. پیش از دیپلوی، فایل `frontend/.env` را با آدرس واقعی بک‌اندتان اصلاح کنید:
   ```
   VITE_API_BASE_URL=https://sabzino-backend.liara.run
   ```
2. دیپلوی از داخل پوشهٔ `frontend/`:
   ```bash
   cd frontend
   liara deploy --app sabzino-frontend --platform react
   ```
   لیارا به‌طور خودکار `npm install` و `npm run build` را اجرا و خروجی `build/` را سرو می‌کند.

3. بعد از دیپلوی، اگر باز شدن مستقیم آدرس‌های داخلی برنامه (مثلاً رفرش کردن صفحهٔ `/requests`) با خطای «یافت نشد» مواجه شد، این یعنی پلتفرم react لیارا به‌صورت پیش‌فرض routing سمت کلاینت (SPA) را مدیریت نمی‌کند — در این حالت از پشتیبانی لیارا دربارهٔ «SPA fallback / rewrite to index.html» برای اپ‌های React سؤال کنید (مستندات رسمی در زمان نوشتن این راهنما این مورد را به‌روشنی توضیح نداده بود).

---

## ۴. تست نهایی

1. آدرس فرانت‌اند (`https://sabzino-frontend.liara.run`) را باز کنید و با یک کاربر آزمایشی وارد شوید.
2. یک درخواست جمع‌آوری واقعی ثبت کنید، با حساب یک راننده وارد شوید و ببینید درخواست در «درخواست‌های نزدیک» راننده دیده و قابل پذیرش/رد است.
3. با حساب مدیر (`is_staff`) وارد `/admin` در برنامه شوید و تب‌های جدید را بررسی کنید:
   - **🚚 حساب رانندگان** — تعلیق/فعال‌سازی مجدد یک راننده
   - **💳 برداشت وجه** — تأیید/رد/ثبت پرداخت یک درخواست برداشت
   - **📋 درخواست‌های جمع‌آوری** — اصلاح آدرس/توضیح، لغو، یا اصلاح وزن‌کشیِ یک درخواست تکمیل‌شده

---

## ۵. نکات امنیتی پیش از استفادهٔ واقعی (نه فقط دمو)

- `SECRET_KEY` باید یک مقدار تصادفی و طولانی باشد، نه مقدار پیش‌فرض توسعه.
- `DEBUG` حتماً باید `False` باشد؛ در غیر این صورت خطاهای سرور برای همه قابل مشاهده است.
- `ALLOWED_HOSTS` را دقیقاً به دامنهٔ واقعی محدود کنید (نه `*`).
- پس از اطمینان از پایداری HTTPS، متغیر `SECURE_HSTS_SECONDS` را (مثلاً به `31536000` معادل یک سال) تنظیم کنید تا HSTS فعال شود.
- پنل جنگو ادمین (`/admin/` در بک‌اند) را با رمز عبور قوی محافظت کنید — دسترسی کامل به همهٔ داده‌ها را می‌دهد.

---

## دامنهٔ اختصاصی (اختیاری)

اگر دامنهٔ اختصاصی (مثل `sabzino.ir`) دارید، از پنل لیارا در بخش «دامنه‌ها»ی هر اپ آن را متصل کنید، سپس `ALLOWED_HOSTS`، `CORS_ALLOWED_ORIGINS`، `CSRF_TRUSTED_ORIGINS` و `VITE_API_BASE_URL` را با دامنهٔ جدید به‌روز و هر دو اپ را دوباره دیپلوی کنید.
