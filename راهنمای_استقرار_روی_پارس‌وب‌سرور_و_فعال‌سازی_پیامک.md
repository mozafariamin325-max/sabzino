# راهنمای بردن سبزینو روی هاست پارس‌وب‌سرور + دامنه eilzagros.ir + فعال‌سازی پیامک

این راهنما دقیقاً برای همان چیزی نوشته شده که خریدی: **هاست «1GB Web Hosting Python Iran» از parswebserver.com** + **دامنه eilzagros.ir**. چون دسترسی SSH/ترمینالت هنوز مشخص نبود، این راهنما اول‌ویت را روی **File Manager + ابزار Setup Python App در cPanel** گذاشته (که خودش نیازی به ترمینال ندارد) و در جاهایی که واقعاً به یک اجرای دستوری نیاز است (migrate دیتابیس)، هم مسیر ترمینال و هم یک **راه‌حل جایگزین بدون ترمینال** (با Cron Job یک‌باره) نوشته شده.

## نقشهٔ کلی چیزی که می‌سازیم

- `https://eilzagros.ir` → فرانت‌اند (فایل‌های استاتیک React/PWA، از طریق public_html همان دامنه اصلی)
- `https://api.eilzagros.ir` → بک‌اند (Django، از طریق ابزار Setup Python App روی یک ساب‌دامین جدا)

جدا نگه‌داشتن فرانت و بک روی دو ساب‌دامین، همان الگوی استانداردی است که روی PythonAnywhere+Vercel هم داشتی — فقط این‌بار هر دو زیر یک دامنهٔ خودت هستند.

**فایل فرانت‌اند آماده شده و از قبل برایت فرستاده شده** (`sabzino-frontend-eilzagros-ir.zip`) — دقیقاً برای آدرس `https://api.eilzagros.ir` بیلد شده، یعنی دیگر لازم نیست خودت Node.js نصب کنی و بیلد بگیری؛ فقط کافی است آن zip را در public_html آپلود و اکسترکت کنی (مرحله ۷).

اگر بعداً اسم ساب‌دامین بک‌اند را چیز دیگری غیر از `api` گذاشتی، فرانت‌اند دیگر با آن صحبت نمی‌کند — چون آدرس بک‌اند داخل فایل‌های بیلدشده «خشک» (baked) شده. در آن صورت باید به من بگویی تا با آدرس درست دوباره برایت بیلد بگیرم.

---

## مرحله ۱ — پیدا کردن اطلاعات ورود cPanel

بعد از خرید هاست از پارس وب سرور، یک ایمیل خوش‌آمدگویی برایت فرستاده شده که شامل این‌هاست:
- آدرس ورود cPanel (چیزی شبیه `https://SERVERNAME:2083` یا `https://eilzagros.ir:2083` یا از طریق «ناحیه کاربری» سایت parswebserver.com)
- نام کاربری و رمز عبور cPanel

اگر پیدایش نکردی، از پنل «ناحیه کاربری» (billing) پارس‌وب‌سرور یا تیکت پشتیبانی‌شان بگیرش — **رمز را هیچ‌جا برای من نفرست**، خودت وارد شو.

بعد از ورود به cPanel، اولین کاری که بکن این است: در جعبهٔ جستجوی بالای cPanel بنویس **Terminal**. اگر یک آیکون Terminal پیدا شد، یعنی به ترمینال مرورگری دسترسی داری (نیازی به نرم‌افزار جدا هم نیست) — این خبر خوبی است، مراحل زیر برایت سریع‌تر می‌شود. اگر پیدا نشد، اشکالی ندارد؛ در مرحلهٔ ۹ یک راه جایگزین بدون ترمینال نوشته‌ام.

---

## مرحله ۲ — تنظیم دامنه eilzagros.ir

دامنه را از کجا خریدی؟

- **اگر از خودِ پارس‌وب‌سرور خریدی**: احتمالاً از قبل به‌صورت خودکار به همین هاست وصل است. برای تأیید، در cPanel برو به بخش **Domains** و ببین `eilzagros.ir` آنجا به‌عنوان دامنهٔ اصلی (Primary Domain) لیست شده باشد.
- **اگر از یک ثبت‌کنندهٔ دیگر خریدی** (مثلاً یک فروشندهٔ دامنهٔ جدا): باید نیم‌سرورهای (Nameserver) دامنه را به نیم‌سرورهای پارس‌وب‌سرور تغییر بدهی. این مقدار دقیق را (چیزی شبیه `ns1.parswebserver.com` / `ns2.parswebserver.com`) از ایمیل خوش‌آمدگویی هاست یا بخش «Nameserver» در ناحیه کاربری خودشان بردار (چون این مقدار می‌تواند بین حساب‌ها/سرورها فرق کند، حدس نمی‌زنم — دقیقش را از خودشان بگیر). این تنظیم را باید در پنل همان جایی که دامنه را خریدی انجام بدهی.

تغییر نیم‌سرور معمولاً بین چند دقیقه تا ۲۴ ساعت طول می‌کشد تا کامل جا بیفتد؛ می‌توانی بقیهٔ مراحل (که همه داخل cPanel است) را همین حالا ادامه بدهی، لازم نیست صبر کنی.

---

## مرحله ۳ — ساخت ساب‌دامین api.eilzagros.ir

در cPanel:
1. برو به بخش **Domains** (یا **Subdomains** در پنل‌های قدیمی‌تر).
2. **Create A New Domain** (یا Subdomain) را بزن.
3. در قسمت Domain بنویس: `api.eilzagros.ir`
4. برای «Document Root» بگذار cPanel خودش پیشنهاد بدهد (چیزی شبیه `api.eilzagros.ir` یا `public_html/api`) — این پوشه فقط یک جای موقت است، چون در مرحلهٔ بعد ابزار Python App مسیر واقعی اجرا را خودش مدیریت می‌کند.
5. **Submit**.

---

## مرحله ۴ — آپلود کد بک‌اند با File Manager

1. فایل `sabzino-mvp-phase11-sms-deploy.zip` که برایت فرستادم را از حالت zip باز نکن — همان‌طور zip نگهش دار.
2. در cPanel برو به **File Manager**.
3. وارد پوشهٔ خانگی‌ات شو (نه public_html — یک پوشهٔ جدا بساز، مثلاً از دکمهٔ **+ Folder** بساز پوشه‌ای به اسم `sabzino_backend`).
4. وارد `sabzino_backend` شو، دکمهٔ **Upload** را بزن و همان zip را آپلود کن.
5. بعد از تمام‌شدن آپلود، به File Manager برگرد، روی فایل zip راست‌کلیک کن → **Extract**.
6. بعد از اکسترکت، محتوای واقعی پروژه داخل `sabzino_backend/backend/` خواهد بود (چون zip کل ریپازیتوری —‌ backend+frontend — را دارد). برای سادگی، محتوای پوشهٔ `sabzino_backend/backend/` را انتخاب کن، **Cut** بزن، و مستقیم داخل `sabzino_backend` (یک پله بالاتر) **Paste** کن — تا در نهایت `manage.py` مستقیم زیر `sabzino_backend/manage.py` باشد، نه دو پوشه پایین‌تر. پوشهٔ خالی‌شدهٔ `backend` و پوشهٔ `frontend` (که برای این هاست لازم نیست) را می‌توانی پاک کنی.

---

## مرحله ۵ — Setup Python App

1. در cPanel جستجو کن: **Setup Python App**.
2. **Create Application** را بزن.
3. **Python version**: بالاترین نسخهٔ موجود نزدیک به ۳.۱۱ را انتخاب کن (اگر ۳.۱۱ نبود، ۳.۱۰ یا ۳.۱۲ هم مشکلی ندارد).
4. **Application root**: `sabzino_backend` (همان پوشه‌ای که در مرحلهٔ قبل ساختی — مسیر نسبی به خانهٔ کاربری، نه از اول `/home/...`).
5. **Application URL**: دامنهٔ `api.eilzagros.ir` را از منو انتخاب کن (بدون هیچ مسیر اضافه بعدش).
6. **Application startup file**: بگذار پیش‌فرض بماند (`passenger_wsgi.py`) — در مرحلهٔ بعد محتوایش را می‌نویسیم.
7. **Application Entry point**: `application`
8. **Create**.

بعد از ساخته‌شدن، cPanel یک جعبهٔ متنی نشانت می‌دهد با عنوانی شبیه «To enter to virtual environment, run the following command» — **این خط را کپی و یک‌جا نگهش دار**، در مراحل بعدی چندبار لازمش داری. چیزی شبیه این است (اعداد/مسیر دقیق مال خودت فرق می‌کند):

```
source /home/USERNAME/virtualenv/sabzino_backend/3.11/bin/activate && cd /home/USERNAME/sabzino_backend
```

---

## مرحله ۶ — نصب پکیج‌ها (pip install)

در همان صفحهٔ Setup Python App، پایین لیست اپلیکیشن‌ها یک دکمه یا لینک هست برای مدیریت اپ که گزینهٔ **Run Pip Install** (یا آیکون مداد ✎ → باز کردن جزئیات اپ → پیدا کردن دکمهٔ نصب پکیج) را دارد. آن را بزن — cPanel به‌صورت خودکار `pip install -r requirements.txt` را از پوشهٔ اپلیکیشن اجرا می‌کند.

اگر این دکمه را پیدا نکردی و به Terminal دسترسی داری (طبق مرحلهٔ ۱)، همان خط فعال‌سازی مرحلهٔ قبل را در Terminal بچسبان و بعدش:
```
pip install -r requirements.txt
```

---

## مرحله ۷ — نوشتن passenger_wsgi.py

در File Manager، فایل `passenger_wsgi.py` را که cPanel داخل `sabzino_backend` ساخته باز کن (Edit) و کل محتوایش را با این جایگزین کن:

```python
import os
import sys

sys.path.insert(0, os.path.dirname(__file__))
os.environ.setdefault("DJANGO_SETTINGS_MODULE", "config.settings")

from django.core.wsgi import get_wsgi_application

application = get_wsgi_application()
```

ذخیره کن.

---

## مرحله ۸ — فایل .env بک‌اند

در همان پوشهٔ `sabzino_backend` (کنار manage.py)، یک فایل جدید بساز به اسم دقیق `.env` (نه .env.example) با این محتوا — مقادیر داخل `<...>` را خودت پر کن:

```
SECRET_KEY=<یک رشتهٔ تصادفی طولانی بساز — مثلاً از https://djecrety.ir بگیر>
DEBUG=False
ALLOWED_HOSTS=api.eilzagros.ir
CORS_ALLOW_ALL=True
CSRF_TRUSTED_ORIGINS=https://eilzagros.ir,https://api.eilzagros.ir

DATABASE_URL=

SABZINO_DEFAULT_COMMISSION_PERCENT=10
SABZINO_DEFAULT_POINTS_PER_KG=2

SMS_IR_API_KEY=
SMS_IR_LINE_NUMBER=
```

(دو خط آخر پیامک را الان خالی بگذار — در بخش «فعال‌سازی پیامک» پایین همین راهنما پرش می‌کنیم.)

`DATABASE_URL` را خالی بگذار تا از SQLite (بدون نیاز به دیتابیس جدا) استفاده کند — دقیقاً مثل PythonAnywhere.

---

## مرحله ۹ — اجرای migrate + seed_demo + collectstatic

اینجا تنها جایی است که واقعاً به یک اجرای دستوری نیاز داریم.

### اگر Terminal داری (مرحلهٔ ۱)
آن را باز کن و این را (با جایگزین‌کردن خط فعال‌سازی خودت از مرحلهٔ ۵) اجرا کن:
```
source /home/USERNAME/virtualenv/sabzino_backend/3.11/bin/activate && cd /home/USERNAME/sabzino_backend
python manage.py migrate
python manage.py seed_demo
python manage.py collectstatic --noinput
```

### اگر Terminal نداری — راه جایگزین با Cron Job
هر هاست cPanel، حتی ارزان‌ترین پلن‌ها، **Cron Jobs** دارد و نیازی به ترمینال ندارد:
1. در cPanel برو به **Cron Jobs**.
2. یک Cron جدید بساز: زمان را روی «هر دقیقه» بگذار (فقط موقتاً).
3. در قسمت Command این را بچسبان (خط فعال‌سازی مرحلهٔ ۵ را جایگزین کن):
```
source /home/USERNAME/virtualenv/sabzino_backend/3.11/bin/activate && cd /home/USERNAME/sabzino_backend && python manage.py migrate && python manage.py seed_demo && python manage.py collectstatic --noinput >> /home/USERNAME/sabzino_backend/deploy_log.txt 2>&1
```
4. Add New Cron Job.
5. یکی دو دقیقه صبر کن تا حداقل یک‌بار اجرا شود، بعد **همان Cron را پاک کن** (وگرنه هر دقیقه دوباره seed را اجرا می‌کند — بی‌خطر است چون seed_demo idempotent است، ولی لازم نیست منابع هاست را هدر بدهی).
6. برای اطمینان از موفقیت، در File Manager فایل `deploy_log.txt` که همین دستور ساخته را باز کن و ببین خطایی نداشته باشد.

بعد از این مرحله، از تب **Setup Python App** دوباره وارد اپ شو و دکمهٔ **Restart** را بزن.

---

## مرحله ۱۰ — آپلود فرانت‌اند

1. فایل `sabzino-frontend-eilzagros-ir.zip` که برایت فرستادم (بیلدشده، آماده) را باز نکن.
2. در File Manager برو به `public_html` (پوشهٔ ریشهٔ دامنهٔ اصلی `eilzagros.ir`، نه ساب‌دامین api).
3. اگر فایل‌های پیش‌فرضی مثل یک `index.html` تبلیغاتی پارس‌وب‌سرور آنجا هست، آن‌ها را پاک کن.
4. zip را در همان public_html آپلود کن، راست‌کلیک → Extract.
5. مطمئن شو بعد از اکسترکت، `index.html` مستقیم زیر `public_html/index.html` باشد (نه یک پوشهٔ میانی اضافه).

---

## مرحله ۱۱ — فعال‌سازی SSL رایگان (HTTPS)

در cPanel برو به بخش **SSL/TLS Status** (یا **AutoSSL** / **Let's Encrypt™**، بسته به نسخهٔ پنل). هر دو `eilzagros.ir` و `api.eilzagros.ir` را تیک بزن و **Run AutoSSL**. معمولاً چند دقیقه طول می‌کشد. تا وقتی سبز نشده، سایت روی `https://` بالا نمی‌آید (مرورگرها امروز HTTP ساده را با هشدار امنیتی نشان می‌دهند).

---

## مرحله ۱۲ — تست نهایی

- `https://api.eilzagros.ir/health/` را باز کن — باید یک پاسخ سالم (نه خطای 500) ببینی.
- `https://eilzagros.ir` را باز کن — باید صفحهٔ ورود سبزینو بیاید.
- با یکی از حساب‌های دمو (`citizen1@sabzino.demo` / `Demo@12345`) وارد شو و مطمئن شو صفحهٔ اصلی و قیمت‌ها لود می‌شوند.

اگر خطای CORS یا 500 دیدی، اول `ALLOWED_HOSTS` و `CSRF_TRUSTED_ORIGINS` در `.env` را دوباره چک کن، بعد از Setup Python App دکمهٔ **Restart** را بزن.

---

# فعال‌سازی پیامک واقعی (اعلان‌های متنی)

طبق چیزی که خواستی، پیامک **فقط برای اعلان‌های متنی** فعال شد (ورود همچنان ایمیل/رمز عبور می‌ماند، هیچ تغییری در آن قسمت داده نشد). با کد جدیدی که در همین فاز نوشتم، دقیقاً این دو اتفاق الان پیامک واقعی می‌فرستند:

1. وقتی یک جمع‌آور درخواست شهروند را می‌پذیرد → پیامک «درخواست شما پذیرفته شد» به شهروند.
2. وقتی وزن‌کشی و تسویه انجام می‌شود → پیامک «مبلغ X تومان به کیف‌پولت واریز شد» به شهروند.

(اعلان نزدیک‌شدن درخواست جدید به جمع‌آورها عمداً پیامکی نشد — چون هر درخواست به‌طور هم‌زمان تا ۸ جمع‌آور نزدیک نوتیفیکیشن می‌گیرد و این یعنی هزینهٔ پیامک زیاد به‌ازای هر درخواست؛ همان اعلان درون‌برنامه‌ای باقی ماند. اگر بعداً خواستی همین را هم پیامکی کنی، یک خط کد است.)

## چرا sms.ir

مستندات API آن‌ها تمیز، عمومی و بدون نیاز به ثبت‌نام پیچیده برای شروع است — فقط یک هدر ساده (`X-API-KEY`) و یک درخواست JSON. اگر بعداً ترجیح دادی از ملی‌پیامک یا ippanel استفاده کنی، چون کد پیامک در یک فایل مستقل (`core/sms_service.py`) نوشته شده، تغییرش به یک provider دیگر ساده و محدود به همین یک فایل است.

## مراحل فعال‌سازی

1. برو به [sms.ir](https://sms.ir) و ثبت‌نام کن (نیاز به شمارهٔ موبایل و معمولاً کد ملی برای احراز هویت پایه دارد).
2. وارد پنل کاربری شو، به بخش **API** یا **وب‌سرویس** برو و یک **API Key** بساز.
3. یک **خط اختصاصی یا اشتراکی** تهیه کن (در پنل sms.ir بخش «خطوط» — برای شروع می‌توانی از خطوط اشتراکی رایگان/ارزان آن‌ها استفاده کنی، لازم نیست حتماً خط اختصاصی گران بخری).
4. شمارهٔ آن خط (فقط عدد، بدون صفر یا کد کشور) را یادداشت کن.
5. برگرد به فایل `.env` بک‌اند (همان‌جا که در مرحلهٔ ۸ ساختی) و این دو خط را پر کن:
```
SMS_IR_API_KEY=<کلید API که از پنل sms.ir گرفتی>
SMS_IR_LINE_NUMBER=<شمارهٔ خط>
```
6. از **Setup Python App** دوباره اپ را **Restart** کن.

از این لحظه، پیامک واقعی برای دو رویداد بالا ارسال می‌شود. اگر `SMS_IR_API_KEY`/`SMS_IR_LINE_NUMBER` را خالی بگذاری، سایت کاملاً عادی کار می‌کند — فقط پیامک ارسال نمی‌شود (هیچ خطایی به کاربر نشان داده نمی‌شود).

---

# چک‌لیست خلاصه

- [ ] دامنه eilzagros.ir به هاست وصل است (نیم‌سرور یا از همان ابتدا خودکار)
- [ ] ساب‌دامین api.eilzagros.ir ساخته شده
- [ ] کد بک‌اند در `sabzino_backend` آپلود و اکسترکت شده
- [ ] Setup Python App ساخته شده و pip install انجام شده
- [ ] passenger_wsgi.py نوشته شده
- [ ] فایل .env بک‌اند ساخته و پر شده
- [ ] migrate + seed_demo + collectstatic اجرا شده (از ترمینال یا Cron یک‌باره)
- [ ] فرانت‌اند در public_html دامنهٔ اصلی آپلود شده
- [ ] SSL هر دو دامنه فعال است
- [ ] ورود دمو تست شده
- [ ] (اختیاری) sms.ir وصل شده و API Key/شماره خط در .env ست شده

اگر در هر مرحله خطای خاصی دیدی، متن دقیق خطا (از مرورگر یا از فایل لاگ cPanel) را برایم بفرست تا دقیق‌تر راهنمایی کنم — حدس‌زدن روی خطاهای هاستینگ معمولاً وقت بیشتری می‌گیرد تا دیدن پیام واقعی.
</content>
